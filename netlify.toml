[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "20", NPM_FLAGS = "--version" }

[build.processing]
  skip_processing = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
